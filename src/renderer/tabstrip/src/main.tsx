import React from "react";
import ReactDOM from "react-dom/client";
import { TabStripApp } from "./TabStripApp";
import "./index.css";

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <TabStripApp />
  </React.StrictMode>,
);
